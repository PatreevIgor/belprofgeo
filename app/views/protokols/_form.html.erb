<meta charset="utf-8"> 
<%= form_for(@protokol) do |f| %>
  <% if @protokol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@protokol.errors.count, "error") %> prohibited this protokol from being saved:</h2>

      <ul>
      <% @protokol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h2>Номер:</h2>
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <h2>Описание:</h2>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
